{
  "name": "leetgrind",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" build && pnpm --filter @leetgrind/server build && pnpm --filter @leetgrind/web build",
    "dev": "pnpm -r --filter \"./packages/**\" build && concurrently -n server,web -c blue,green \"pnpm --filter @leetgrind/server dev\" \"pnpm --filter @leetgrind/web dev\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r --filter \"./packages/**\" build && pnpm --filter @leetgrind/server build && pnpm -r --filter \"./packages/**\" typecheck && pnpm -r --filter \"./apps/**\" typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/supertest": "^7.2.0",
    "@vitejs/plugin-react": "^6.0.1",
    "@vitest/coverage-v8": "^4.1.5",
    "concurrently": "^9.2.1",
    "jsdom": "^29.0.2",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.5"
  }
}
